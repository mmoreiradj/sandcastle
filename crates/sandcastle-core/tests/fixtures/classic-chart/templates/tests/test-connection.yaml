apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "classic-chart.fullname" . }}-test-connection"
  labels:
    {{- include "classic-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "classic-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
